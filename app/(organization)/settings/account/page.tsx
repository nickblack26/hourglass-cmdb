import DeleteAccount from '@/components/delete-account';
import React from 'react';

type Props = {};

const Page = (props: Props) => {
	return (
		<div>
			<DeleteAccount />
		</div>
	);
};

export default Page;
